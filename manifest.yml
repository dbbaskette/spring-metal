---
applications:
- name: spring-metal
  path: ./target/spring-metal-1.3.1.jar
  buildpack: java_buildpack_offline
  random-route: true
  env:
    JBP_CONFIG_OPEN_JDK_JRE: '{ jre: { version: 21.+ } }'
    SPRING_PROFILES_ACTIVE: cloud,mcp

  services:
    - boneyard-db
    - audiodb-mcp-server